# Cloudflare Pages 重定向规则
# 参考: https://developers.cloudflare.com/pages/platform/redirects/

# 注意：Hugo 已经配置为生成带尾部斜杠的URL，所以不需要额外的重定向规则
# 这里只保留必要的重定向规则

# 处理旧的URL格式（如果存在不带尾部斜杠的旧链接）
# 只有当目标URL确实存在时才重定向，避免循环

# 处理RSS和sitemap
/sitemap.xml /sitemap.xml 200
/zh/sitemap.xml /zh/sitemap.xml 200
/en/sitemap.xml /en/sitemap.xml 200

# 处理robots.txt
/robots.txt /robots.txt 200

# 处理favicon和静态资源
/favicon.ico /favicon.ico 200
/android-chrome-*.png /android-chrome-192x192.png 200
/apple-touch-icon.png /apple-touch-icon.png 200
/site.webmanifest /site.webmanifest 200

# 处理404页面
/* /404.html 404
